// AppType is imported directly from the BFF's source (apps/platform-api/src/app.ts) by the
// frontend Hono RPC client via the @ae-hq/api alias (configured in vite.config.ts +
// apps/platform-app/tsconfig.json paths).

// Sentinel type so this file is treated as a module.
export type _ = never;
